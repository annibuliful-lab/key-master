overwrite: true
schema: 'http://localhost:4000/graphql'
documents: 'apps/dashboard/graphql/**/*.gql'
generates:
  apps/dashboard/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
